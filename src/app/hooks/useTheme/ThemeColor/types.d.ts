export interface BaseColors {
  primary: string;
  secondary: string;
  text: string;
  background: string;
}

export interface CalculatedThemeColors {
  primary: string;
  secondary: string;
  theme: string;
  themeBackground: string;
}
